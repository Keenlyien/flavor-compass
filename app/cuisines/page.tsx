import CuisinesView from "@/components/cuisines/CuisinesView"

export const metadata = {
  title: "Cuisines — Flavor Compass",
  description: "Discover cuisines from around the world, with a fun fact behind every dish.",
}

export default function CuisinesPage() {
  return <CuisinesView />
}
